const user = { first : 'max' , last : 'way'  };
const {first, last} = user;
console.log(first, last);

// const setA = new Set([1,2,3]);
// const setB = new Set([3,4]);

// console.log(setA.union(setB));