// 삼항 연산자 
let a = 2
let ex = a > 4 ? 'true' : 'false'; // 조건식 ? 조건식이 ture일때 반환 값 : 조건이 false 일때 반환 값 
console.log(ex);

//p.96 
let x = 3
let result = x%2 ? '홀수' : '짝수'; // 1은 true, 0는 false 라서 

console.log(result);